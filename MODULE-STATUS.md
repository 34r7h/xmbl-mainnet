# MODULE-STATUS — every XMBL unit: working · tested · reproducible in a miniapp

Standing requirement (operator, 2026-09-12): **no stop until every module in `xmbl-mainnet` is
working, tested, and reproducible in a miniapp.** This file is the durable checklist that defines
"done" for that program and survives context compaction. It is enforced by
`tests/module-status.test.mjs`, which discovers every package/app/crate from the filesystem and
**fails the build if any unit has no row here** — so a unit can never be added or forgotten without
being accounted for (the same filesystem-discovering pattern as `crates/crate-status.test.mjs`).

The three states are **independent** and each is claimed only when true:

- **Working** — the real code path runs and produces correct output (not a stub, not a throw).
- **Tested** — a self-contained `*.test.mjs`/suite asserts the behavior and exits non-zero on
  failure; "✓ (N, gate)" means the suite is in the `npm run test:protocol` hard gate with N files.
- **Reproducible in a miniapp** — a **self-contained, content-addressed reproduction bundle** that
  exercises the module's REAL code and lets an auditor reproduce the claim with one command,
  verified by a harness in the `apps/app-builder/miniapp/verify.mjs` pattern. For modules that are
  **Node-only by construction** (worker-thread isolation + CPU metering; real sockets) the bundle is
  a Node reproduction run with `node`, not a browser-surface bundle — rewriting those to the browser
  surface would delete the very isolation/metering property the module exists to provide. This
  Node-vs-browser split is an operator-reversible interpretation of "miniapp"; flag if wrong.

Legend: ✓ done · ◐ partial · ✗ not yet · n/a not applicable · ⛔ blocked on external audit (see
`MAINNET-GATES.md`).

---

## Protocol packages (JS/WASM reference implementation)

| Unit | Working | Tested | Miniapp reproduction | Notes |
|------|---------|--------|----------------------|-------|
| `packages/core` | ✓ | ✓ (3, gate) | ✓ | boot/profile gate; `AUDIT_GATES_OPEN` refuses mainnet boot until ⛔ gates close. **2026-09-16 coherence pass:** ships the node daemon as the `xmbl-node` bin (`bin/xmbl-node.js`, the SAME daemon the handoff coordinators supervise — previously only a vendored copy of `core/` in the node bundle, drifting from this package) with `control-socket.js`/`node-config.js`/`metrics-server.js` as subpath exports; the control socket now implements the coordinator's full contract — op `xsc` (was mis-renamed to `'@xmbl/storage-compute'` by the scaffold's package rename), `submit_batch` (`params.txs[]` → `{ok, accepted, failed, total, results[]}`), `ledger_capabilities` (`{ok, capabilities:{rescues_non_anchor_blocks,…}}`, the only live grant for `rebuild_ledger`), `identity_status` (`Identity.verifySigning()` — the crossed-keypair detector), a SIGNED `chain` claim (`stmt/sig/pk`, self-certifying address) and `status.versions` = the @xmbl versions this process LOADED (every package exports a load-time `VERSION`); `submit_tx` returns `ok:false` on an ingress rejection instead of `ok:true, tx_id:null`; `start()` awaits `ledger.ready()`/`stateMachine.ready()` so nothing reads a store mid-rehydration **Rollout policy (2026-09-16):** `release.js` version proof (`status.build`, signed into the `chain` claim; `release` op), `core.suspend()` stops production behind the latest published version, `xmbl-node` OTA loop installs the latest `@xmbl/core` and restarts (exit 75 under a supervisor) — suites 5 | **Reproduction (B3, 2026-09-16):** reproductions/core.mjs — proves the code it runs, refuses to produce while behind, resumes , plus `reproductions/three-nodes.mjs` — three full nodes converge under adversarial delivery (B2) |
| `packages/identity` | ✓ | ✓ (5, gate) | ✓ | `CubicCurveSource` is the real construction (`secure:false, audited:false` until ⛔ audit); delegation/seal present. Cubic-LWE is additively HOMOMORPHIC (`addCiphertexts`, gate-tested over 50 trials) and **now CONTRACT-WIRED**: `@xmbl/contracts`' `heHost` exposes it as `env.xmbl_he_add`, so a contract can combine ciphertexts it cannot read — DECRYPTION stays off-host (needs the secret key), reproduced by `reproductions/contract-he.mjs` | **Reproduction (B3, 2026-09-16):** reproductions/identity.mjs — self-certifying address, signature binds every other field, crossed keypair detected |
| `packages/cubic-ledger` | ✓ | ✓ (12, gate) | ✓ | UTXO cube-of-cubes ledger. Block ids address CONSENSUS CONTENT (`consensusBody`; `hash` stays the whole-tx wire contract); an anchor's `hash` must be a sha-256 digest; an invalid tx is EVICTED for good (`evict`, durable `evicted:` keyspace, refused on every path across restarts and canonical rebuilds); a canonical rebuild preserves every non-anchor block and reports what it wiped; **legacy envelope-keyed rows converge to content ids on the next boot by themselves** (`content-id-rekey-on-boot.test.mjs` — the compaction was a method nobody called; it also evicts a row today's rule refuses instead of aborting); `addTransaction` returns the pooled block's `{id, hash}`; `ready()` is the boot join point. Removed as slop: `capabilities()` (three hardcoded `true`s) and `census.mjs` (an operator one-off that shipped in the tarball) **2026-09-16 typed protocol:** every tx is typed by its xid (tokens.json codes; `micromineTx`/`validateXid`), untyped rows are deleted on boot and skipped by a canonical rebuild (counted), the block hash is CONTENT-ONLY so every node seals the same cubes, `verifyPlacement` is the geometric-placement stage every seal and adoption asserts | **Reproduction (B3, 2026-09-16):** reproductions/cubic-ledger.mjs — same set → same chain in any order, nothing outside the set destroyed, a wipe refused, a forgery cannot evict its victim |
| `packages/state-machine` | ✓ | ✓ (5, gate) | ✓ | Verkle state machine. Trie rebuilt on load (was 64 zeros after every restart), diffs keyed by content identity, `ready()`. `apply-path.test.mjs` fixtures mine real digests (was RED after the anchor-digest rule landed with the gate never run) | **Reproduction (B3, 2026-09-16):** reproductions/state-machine.mjs — same root in any order, a divergent node converges, root + applied count survive a restart |
| `packages/consensus` | ✓ | ✓ (3, gate) | ✓ | User-as-validator, five-stage mempool. A content-addressed type-6 value tx is admitted on its content address (no in-body sig by design — tokens.json); a malformed anchor is refused AT THE DOOR by cubic-ledger's own `validateTransaction` (one rule, one place — the ledger would only evict it after consensus paid for it); `finalizeTransaction` preserves the signed `id`. Import-time port banner removed (nothing listens; it polluted every consumer's stdout) **2026-09-16:** `validate.js` runs the operator's order at the door — 1. can it happen, 2. is the xid correct, 3. (seal/adopt) placement — and names the failing stage in every refusal **A5 (2026-09-16):** the quorum clock is a SIBLING of the tx, never a field inside it — a finalized tx re-verifies against its signer's key | **Reproduction (B3, 2026-09-16):** reproductions/consensus.mjs — each stage refuses what it owns and the first failing stage is the one reported |
| `packages/storage-compute` | ✓ | ✓ (3, gate) | ✓ | **Node-only by construction** — worker isolation, `process.threadCpuUsage`, killed-job billing, node:sqlite. `CoordinateDelivery` (dead: `require` in ESM, "encryption" keyed on the recipient's PUBLIC key) is removed; import-time port banner removed | **Reproduction (B3, 2026-09-16):** reproductions/storage-compute.mjs — a looping guest is killed at the operator’s deadline and still billed; custody proved from the bytes |
| `packages/zero-knowledge` | ✓ | ✓ (1, gate) | ✓ | FRI experimental/unaudited ⛔. **Now CONTRACT-WIRED** as an opt-in host call: `@xmbl/contracts`' `zkHost` deploy flag exposes `verify` to a deployed contract as a synchronous `env.xmbl_zk_verify(x_ptr, y_ptr)` (proof + public points chain-staged for a deterministic verdict; the guest supplies the asserted coordinate from its own memory, so the verdict binds to the contract's state, not a host flag — `src/xcl/abi.js` `HOST_ABI_ZK_INIT_SOURCE`). Reproduced by `reproductions/contract-zk.mjs` (hard gate): a contract gates a Verkle write on a real coordinate/curve proof — the root MOVES on a verified coordinate, is UNMOVED on a tampered one, a malformed proof refuses without trapping, the unflagged import is denied, and two nodes converge to one root. Also covered by 4 `contract-host.test.mjs` cases (gate). **OPT-IN per contract and MUST NOT gate consensus/ledger/sealing** — a contract that does not opt in never touches it |
| `packages/networking` | ◐ | ✓ (1, gate) | ✓ | **Node-only by construction** — discovery/gossip under NAT is ⛔ integration/audit | **Reproduction (B3, 2026-09-16):** reproductions/networking.mjs — two real libp2p nodes dial by peer id and a published message arrives intact |
| `packages/lng` | ✓ | ✓ (11, gate) | ✓ | LNG→WASM compiler + XCL compose backend; **EVM backend output is DEPLOYED and EXECUTED in the gate** (`evm-deploy.test.mjs` 16/16: transpile → in-process solc → bytecode into @ethereumjs/evm, three-way parity interpreter == WASM == EVM, reverts carry the LNG reason; solc compile in `transpile-evm.test.mjs` is unconditional now — both dev-only dependencies, nothing ships). **Ships its browser build** (`dist/lng.browser.js` = `@xmbl/lng/browser`): one dependency-free ES module generated from the SAME `src/*.js` by `build-browser.mjs`, byte-checked by `browser-bundle.test.mjs` (34/34: same surface, same bytes, runs with no Node globals) and loaded in a real Chromium 153 (import → run, 5664-byte WASM instantiates, Solidity transpiles) — nobody ports LNG by hand | **Reproduction (B3, 2026-09-16):** reproductions/lng.mjs — interpreter == WASM on every call, real Solidity out, non-determinism cannot compile |
| `packages/contracts` | ✓ | ✓ (5, gate) | ✓ | XCL agentic-contract runtime (ContractHost); `reproductions/agentic-contract-e2e.mjs` hosts a gated contract, drives EVERY entrypoint (machine-checked vs WASM exports) through the real root→coordinator→agent chain, and proves state updating at every surface (fields, Verkle root, UTXO ledger) with fail-closed refusals leaving the root unmoved, Verkle-provable changes, conservation, and cross-node determinism; `reproductions/contracts-reentrancy.mjs` covers reentrancy-by-construction; `reproductions/contract-zk.mjs` proves a contract USES xmbl's coordinate/curve zero-knowledge (`@xmbl/zero-knowledge`) to gate a Verkle write (opt-in `zkHost`, still ⛔ unaudited); `reproductions/contract-he.mjs` proves a contract homomorphically ADDS post-quantum cubic-LWE ciphertexts it cannot read and persists the encrypted aggregate (opt-in `heHost` → `env.xmbl_he_add`; decryption is exposed to no contract); `reproductions/contract-usdc-settlement.mjs` COMPOSES zkHost + heHost + `@xmbl/identity` sealing to settle USDC on a network the user chooses — the zk release coordinate IS the settlement-record hash (`sha256({receiver,asset,amount,chain,nonce})` reduced into the FRI field), so a `base` proof cannot release the same settlement re-pointed at `ethereum` (root unmoved); the amount nets under `env.xmbl_he_add`; the rail's authorizing EVM key is sealed (PQ Cubic-LWE at `MAINNET_N`) to the receiver and never custodied on-chain, with wrong-receiver, mutated-amount, and toy-ring value-seal all refused. **Executes guest bytecode via `storage-compute`, whose worker isolation is a ⛔ external-audit gate** — contracts' own logic is clean/reproduced but transitively rests on that audit |

## Tooling / client packages

| Unit | Working | Tested | Miniapp reproduction | Notes |
|------|---------|--------|----------------------|-------|
| `packages/cli` | ◐ | ✓ (11, gate — 41/41) | ✗ | Imports its modules BY PACKAGE NAME (was monorepo-relative paths — the npm-published CLI could not resolve them); stdout is a clean result channel (library logs → stderr; every two-word command like `ledger tx add` was a commander positional-argument bug that handed the word `add` to the action as its options); `consensus submit --key <name>` signs before submitting and reports an ingress refusal as `ok:false`/exit 1; **`chain start` runs the REAL `LocalDevnet` + `DevnetRpc` as a detached daemon** (10 named accounts funded by a faucet with real signed transfers; `accounts/account/balance/status/stop/reset` are RPC clients of it) — the old in-process mock died with the shell that started it. **In the hard gate since 2026-09-16 (B4)**: `jest-suite.test.mjs` runs the real jest suite in a child process and exits with its status, so a CLI regression fails the build |
| `packages/browser-extension` | ✓ | ✓ (3, NOT in protocol gate) | ✓ | **Revived** Chromium-MV3 wallet & contract client (Vue 3 + webpack, reskinned in the miniapp's **impeccable.style**). `dist/` is **gitignored** and `manifest.json` loads `dist/{background,content,popup}.js`, so `npm run build -w packages/browser-extension` is **required before loading unpacked** (documented in the package readme). Build was broken and fixed: ESM/CJS webpack-config mismatch (`git mv webpack.config.js .cjs`) + `@babel/preset-env` mangling `@xmbl/lng`'s BigInt literals (babel-loader removed; webpack 5 parses modern ESM natively). A **Contracts** tab creates, deploys, finds and calls contracts entirely in-page: it inlines the REAL `@xmbl/lng` compiler and the miniapp's verified in-page executor (`src/contract-runtime.js`), deploys to a `browser.storage` registry, and runs compiled WASM over a local Verkle stand-in. `tests/contract-runtime.parity.test.mjs` (the package's `npm test`) proves the extension's content-addressed id and coordinates are **byte-identical to node `@xmbl/contracts`** for every sample, plus real execution and a trapping-revert. Two durable harnesses back this: the **node** parity test above, and `verify-extension.mjs` (`npm run verify:extension`), a **browser-surface** harness in the `verify-contract-lab.mjs` pattern (Playwright chromium) that drives the REAL built `dist/popup.js` through every workflow — shell/tabs, create+compile all 3 samples, deploy (content-addressed id written to `browser.storage`), find+search, call (committed-state moves; a Vault over-withdraw REVERTS leaving state unmoved), and the Wallet tab — and asserts zero page/console errors (**24 checks, 0 failed**). Only the two extension HOST APIs are shimmed in `harness.html` (`chrome.storage.local` with chrome's callback semantics; `chrome.runtime.sendMessage` emulating the node bridge's responses when connected to a fresh devnet, so the popup workflow runs without spawning one), so all popup UI, `@xmbl/lng` compile and contract execution run unmodified. Needs a chromium binary, so it is run on demand, not in the protocol gate. **Load-unpacked fix:** Chrome refused to load the unpacked extension because the tree held `_`-prefixed names (`__tests__/`, `_harness.html`) — reserved by Chrome (extensions/common/file_util.cc); renamed to `tests/` and `harness.html`, and `tests/assert-loadable.mjs` (run by `npm test` and post-build `verify:extension`) now fails on any `_`-prefixed entry anywhere in the tree (Chrome's exact rule; `_locales`/`_metadata`/`_platform_specific` excepted). Headless Chrome-for-Testing's CLI `--load-extension` does not surface this error, so the filename-invariant guard — not a browser check — is the faithful verification. **Blank-popup fix (clicking the icon did nothing):** the webpack config was `mode:'development'`, whose default `devtool:'eval'` wraps every module in `eval()`, and there was no manifest `content_security_policy` — under the default MV3 extension-page CSP `eval`/`new Function`/WASM are forbidden, so the popup's bundle died at load and rendered blank; the Playwright popup-harness is a normal page with no such CSP, so it never caught it. Fixed: `devtool:false` (no eval, non-minified so `@xmbl/lng`'s BigInts survive), `vue$`→runtime-only build (drops the `new Function` template compiler), and `manifest.content_security_policy.extension_pages = "script-src 'self' 'wasm-unsafe-eval'; object-src 'self'"` (allows `@xmbl/lng` WASM). Proven by `tests/verify-extension-loaded.mjs` (in `verify:extension`): loads the REAL unpacked extension in chromium under the REAL MV3 CSP, opens `popup.html` as the toolbar icon does, and asserts the Vue app MOUNTS (`#shell`) with zero CSP/console errors, saving screenshots to `tests/screenshots/`. Both harnesses now emit screenshots (workflow steps + the loaded popup's Contracts/Wallet tabs). **In-page execution ≠ production** (no worker isolation/metering/Verkle commitment/delegation — stated on screen). The **Wallet tab is no longer a stub**: `src/background.js` proxies its five wallet/node messages over loopback HTTP to a running **XMBL LocalDevnet** RPC (`npm run devnet -w packages/simulator`), so balance = net of applied deltas, a send lands a real signed+verified tx, and status reports the real running/peers/height; with no devnet reachable it reports a truthful **disconnected** state (balance 0, `connected:false`, send errors) and never fabricates. Default URL `http://127.0.0.1:8646`, overridable via `browser.storage.local` key `xmbl:devnetUrl`. Proven end-to-end against a real devnet RPC by `tests/background-bridge.test.mjs` (**23 checks**, in `npm test`). **Extended to the full XMBL Console (operator: "the extension should have ALL parts of xmbl-mainnet available to use and config"):** the popup is now **five tabs** — **Contracts** (the full lab: a **Code ⇄ Visual builder** via `src/contract-builder.js` = the miniapp's `parseToModel`/`modelToSource`, the full **5-sample** set Counter/Vault/Calc/Logic/Ledger **seeded on first open** so search/use works immediately — and removing every instance is never a dead end, the empty list carries a **Load examples** restore button — deploy/find/call with state tiles + reverting traps), **Crypto** (the opt-in host-capability surface — cryptoHost signature-verify, zkHost coordinate/curve proof, heHost encrypted-add, and zkHost+heHost+seal USDC settlement — each a REAL primitive run in the devnet via `packages/simulator/src/capabilities.js`, returning a verdict **and its negative control**, never a fabricated pass; disabled + "no devnet" when unreachable), **Wallet**, **Node** (live status, ledger **state root**, and the map of every xmbl module and where it runs), and **Config** (the `xmbl:devnetUrl` endpoint via a `setDevnetUrl` bridge message). `src/background.js` proxies the new `getStateRoot`/`zkProof`/`heAdd`/`sigVerify`/`seal`/`get`+`setDevnetUrl` messages, truthfully disconnected when no devnet. The browser-surface harness is now **31 checks** (including the remove→**Load examples** restore round-trip, so an emptied Find list can never permanently break) and `tests/verify-extension-loaded.mjs` (real unpacked extension, real MV3 CSP) is **21 checks** — it screenshots all five tabs disconnected, then **stands up a real LocalDevnet on `:8646`** and asserts the Crypto tab yields REAL green **zk** and **HE** verdicts and Wallet/Node read live state (screenshots `01`–`08` in `tests/screenshots/`). Not in the `test:protocol` gate (a client package; that gate is protocol-only) |
| `packages/desktop-app` | ◐ | ✓ (2, gate — 5/5) | ✗ | **Fixed + gated 2026-09-16 (B4), three real defects:** `main/main.js` exported the constructed INSTANCE (so `new MainProcess()` threw "not a constructor") and booted a node and an Electron app as an import side effect — it exports the class now and starts only as the entry point; `createWindow()` never returned the window it made; and `main/`, `preload/`, `src/` and `__tests__/` hold CommonJS files under a `"type": "module"` package, which is why the suite could not load. A recording `electron` double (`__mocks__/electron.cjs`) lets the real main-process code run outside Electron |
| `packages/simulator` | ✓ | ✓ (2, gate) | ◐ | **Revived as `LocalDevnet` (src/devnet.js) — the "hardhat for XMBL".** A scripted, bounded local network over the REAL modules (`@xmbl/identity` MAYO keys + `@xmbl/cubic-ledger`) with ledger-side signature verification **ON** (wires both `xid` and `getPublicKeyByAddress`). `src/devnet.test.mjs` (in the `test:protocol` hard gate) drives the pipeline and asserts the OUTCOME by count: 4 real identities minted, 12 signed `utxo` transfers submitted → **12 landed**, a 9-block **face sealed**, a state root present, balances equal to the net of applied deltas, and a **negative control** (a tx tampered after signing is REJECTED — verification is live). It submits via the **direct `ledger.addTransaction` path**, and also drives `consensus.finalizeTransaction` directly, to prove the consensus→ledger signature seams (below). A loopback **RPC** (`src/devnet-rpc.js`, `DevnetRpc`) serves the browser-extension's exact five-message background contract (`getBalance`/`sendTransaction`/`getNodeStatus`/`startNode`/`stopNode`) from REAL devnet state — gated by `src/devnet-rpc.test.mjs` (**23 checks**: balance = net of applied deltas, a real signed tx lands and moves balances/height, lifecycle toggles, plus the live **state root** and the node-side **capability surface**, including that `heAdd` rejects a non-bit operand rather than coercing it to a false green verdict). The RPC also serves that capability surface (`src/capabilities.js`): `zkProof` / `heAdd` / `sigVerify` / `seal` each run a REAL `@xmbl/zero-knowledge` or `@xmbl/identity` primitive end-to-end in the node process and return a JSON-safe verdict **with its negative control** (a tampered coordinate/message rejected, ENC(1)⊞ENC(1)=0 mod 2), mirroring the `@xmbl/contracts` reproductions — this is how the browser-extension's **Crypto** tab uses xmbl zk/HE/signature/seal for real. Run it with `npm run devnet -w packages/simulator` (`src/devnet-run.mjs`, prints the URL). The legacy `SystemSimulator` (random interval soak) remains (`npm run soak`). The extension's `background.js` now bridges to this RPC (proven by `browser-extension/tests/background-bridge.test.mjs`). Two of the three consensus→ledger verification seams are now **FIXED** (proven by `src/devnet.test.mjs`, each with a live negative control): (a) `finalizeTransaction` no longer overwrites the signed `id`, and (b) `addSealedBatch` now verifies via `Identity.verifyTransaction` instead of a nonexistent `this.xid.verify`. REMAINING (◐): one audit-scoped seam — `moveToProcessing` injects `validationTimestamp` into the signed body, so enabling ledger-side re-verification on the FULL consensus path needs a signature-domain / block-id decision (`DEVNET-SEAM-FINDING.md` defect (c)); `src/devnet.test.mjs` PINS it (asserts a tx carrying an injected `validationTimestamp` does NOT re-verify), so the gate trips if anyone enables verification without resolving it. Ledger-side re-verification stays OFF in the production daemon (`core/index.js` builds the `Ledger` without `getPublicKeyByAddress`, so the lookup returns null and verification is skipped) until (c) is decided |
| `packages/visualizer` | ◐ | ✗ (0) | ✗ | Vue 3 + three.js + socket.io real-time visualizer over a bridge `server.js`. **Was broken** — `server.js` imported `SystemSimulator` from `../xsim/index.js` (the package was renamed to `simulator`), so the server threw `ERR_MODULE_NOT_FOUND` on boot; fixed to `../simulator/index.js`. It now boots and connects the REAL modules (identity, networking, cubic-ledger, state-machine). Package manifest was also lying (`main`/`exports` → a nonexistent `index.js`); corrected to `main: server.js` and marked `private` (it is an app, not a published library). Still ✗ tests — no gated suite; a client/observability app, not a protocol module |

## Apps

| Unit | Working | Tested | Miniapp reproduction | Notes |
|------|---------|--------|----------------------|-------|
| `apps/app-builder` | ✓ | ◐ (2, vitest/playwright, NOT in gate) | ✓ | `miniapp/contract-lab.{js,html}` + `samples.js` is the **XMBL Contract Lab** handoff miniapp (live at `handoff.lol/app/xmbl/contract-lab`, v2.2): a full contract **builder** — a **Visual builder** and a **Code** editor over ONE model (`parseToModel`/`modelToSource`, AST-round-tripped), a **Test** panel whose committed-state tiles flash as each call writes them (with per-call return value + `__events()` count), and a **Deploy** button that commits a live content-addressed instance and switches Test onto it. The v2.2 redesign adopts the **impeccable.style** system (Albert Sans + JetBrains Mono, oklch "paper" neutrals, KINPAKU gold spent sparingly as an LED on only the primary action / focus / live-and-flash indicators, PATINA teal for structure, a dark **instrument** Test console with LED readout tiles and key-cap Run buttons, physical cap-lift buttons and track-recessed inputs, hairline rules, no nested cards / icon tiles / soft-radius cards) and makes the builder intuitive: each statement reads as a plain-language sentence row (Set · field · to · expr / Return · the value / Emit · event / If · condition / Repeat · counter · from · to), operand boxes suggest every name in scope, statements are added by labeled **Set/Local/Return/Emit/If/Repeat** actions (no create-then-reclassify), and a live **Generated LNG** preview updates as you build. The builder inputs are **borderless-at-rest prose** — an editable value is told from a connective word by typeface alone, and border/recess/fill appear only on hover/focus (measured: 36→0 `.in/.sel` boxes drawing chrome at rest in the default Counter builder), so the builder reads as sentences instead of a wall of input boxes. A read-only **Host capabilities** panel documents the three node-side, opt-in host calls a deployed contract may declare — `cryptoHost` (`env.xmbl_cubic_sig_verify` / `env.xmbl_mayo_verify`), `zkHost` (`env.xmbl_zk_verify`), `heHost` (`env.xmbl_he_add`) — each with its ABI and the exact headless reproduction that proves it; it is deliberately NOT an in-page control (the lab compiles LNG, which emits no crypto host imports, and cannot bind real xmbl zk/HE in-page — both need `node:crypto`), so the capability is surfaced honestly and proven headlessly, decryption on no ABI. It inlines the REAL `@xmbl/lng` compiler and runs the compiled WASM in-page via `WebAssembly.instantiate` over a faithful copy of the XCL byte-pointer state ABI + word marshal (`abi.js`). Five worked examples (`samples.js`) cover **every call type the host-state backend compiles**: state writes, no-/single-/multi-arg calls, getters, every arithmetic (`+ - * / %` — over/underflow & div0 REVERT) / bitwise (`b& b| b^`) / shift (`b< b>`) op, comparisons (`== !== !< !>`), a ternary branch, an `~event`+`~emit`, a `~private` field, and a counted `~for` loop. `verify-contract-lab.mjs` proves it in three phases: (1) **functionality** — every entrypoint of every sample EXECUTED and asserted (38 assertions across 25 entrypoints, incl. revert cases), (2) source→visual-model→source **round-trip** (5/5), (3) **both handoff surfaces** (opaque-origin iframe + shadow-DOM `renderApp`) build→test (a call updates a state tile)→deploy (live instance) with the in-browser id byte-identical to `@xmbl/contracts` `contractId` in node. **Test mode ≠ production execution** (no worker isolation / metering / Verkle commitment / delegation gate — stated on screen); the full gated path is the headless `reproductions/agentic-contract-e2e.mjs`. Build+verify: `npm run build:contract-lab && npm run verify:contract-lab -w apps/app-builder`. **`verify-contract-lab.mjs` is NOT in the `npm run test:protocol` hard gate** — it needs a built `dist-contract-lab/` bundle and a chromium binary (Playwright), so it is run on demand; a regression in `contract-lab.js`/`samples.js` is caught only when the build+verify command above is re-run. The older `verify.mjs` two-surface harness (visual app builder) also remains, likewise out of the gate. |
| `apps/visualizer` | ◐ | ✗ (0) | ✗ | |

## Rust crates (crates.io targets)

All eight are **NON-PRODUCTION pre-mainnet stubs** (labeled in each `src/lib.rs`, guarded by
`crates/crate-status.test.mjs`, 9/9). They have NOT reached parity with the JS reference and are
blocked on that parity work before any crates.io consumer can rely on them.

| Unit | Working | Tested | Miniapp reproduction | Notes |
|------|---------|--------|----------------------|-------|
| `crates/xmbl-core` | ✗ stub | label-guarded | n/a | parity-with-JS pending |
| `crates/xmbl-identity` | ✗ stub | label-guarded | n/a | parity-with-JS pending |
| `crates/xmbl-cubic-ledger` | ✗ stub | label-guarded | n/a | parity-with-JS pending |
| `crates/xmbl-state-machine` | ✗ stub | label-guarded | n/a | parity-with-JS pending |
| `crates/xmbl-consensus` | ✗ stub | label-guarded | n/a | parity-with-JS pending |
| `crates/xmbl-storage-compute` | ✗ stub | label-guarded | n/a | parity-with-JS pending |
| `crates/xmbl-zero-knowledge` | ✗ stub | label-guarded | n/a | parity-with-JS pending |
| `crates/xmbl-networking` | ✗ stub | label-guarded | n/a | parity-with-JS pending |

---

## Open work to close this program (in priority order)

1. **Miniapp reproductions** — no protocol module yet has an auditor-runnable reproduction bundle;
   only `apps/app-builder` has the `miniapp/` harness, and it demonstrates app-render, not protocol.
   Build one reproduction per module in the `verify.mjs` pattern (browser surface where the module
   is browser-capable; Node reproduction for `storage-compute`/`networking`).
2. **`simulator`** — DONE: revived as `LocalDevnet` + `DevnetRpc` with two gated suites (`src/devnet.test.mjs`, `src/devnet-rpc.test.mjs`, in `test:protocol`) and a runner (`npm run devnet`); the extension's `background.js` now bridges to this RPC (proven by `browser-extension/tests/background-bridge.test.mjs`). Two of the three consensus→ledger seams are now FIXED (id-overwrite in `finalizeTransaction`; the nonexistent-method call in `addSealedBatch`). Remaining: one audit-level seam — `moveToProcessing` injects `validationTimestamp` into the signed body, gating ledger-side re-verification on the full consensus path on a signature-domain / block-id decision (§ `packages/simulator/DEVNET-SEAM-FINDING.md` defect (c)).
3. **`visualizer` (package)** — server boots against the real modules (data paths were broken by the package rename: `data/@xmbl/simulator/…` for the simulator's `data/xsim/…`; fixed); still no gated suite — define its contract or retire it.
4. **Enforce `cli`/`browser-extension`/`desktop-app` tests** — they exist but the hard gate never
   runs them, so regressions land silently; add them to `test:protocol` (cli is green as of 2026-09-16;
   desktop-app's `main.test.js` needs the `MainProcess` export fixed first).
5. **Release discipline** — the 2026-09-15/16 hotfix spray (per-package hand publishes, `.npmrc` token,
   gate never run, crates left at 0.1.0) is closed by the 0.1.11 line; RELEASING.md now forbids hand
   publishing. The node bundle (`xmbl-slim-node`) still vendors a copy of `core/` — it should depend on
   `@xmbl/core` and run `xmbl-node` (handoff-claude's side).
5. **`networking`** — NAT discovery / gossip fan-out is a ⛔ integration/audit gate.
6. **Rust crate parity** — eight stubs must reach JS-reference parity (or stay labeled non-production).
