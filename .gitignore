# deps
node_modules/
# js builds
dist/
# ...except @xmbl/lng's browser build: committed and byte-checked against its sources by the gate
!packages/lng/dist/
dist-node/
dist-miniapp/
dist-contract-lab/
build/
*.tsbuildinfo
# rust builds
target/
Cargo.lock
# runtime chain state (deployment, never source)
data/
*.leveldb/
keys/
*.pid
*.sock
# packaging output
release-artifacts/
*.dmg
*.AppImage
*.exe
*.deb
*.rpm
# env / local
.env
.env.*
!.env.example
.DS_Store
coverage/
*.log
.npmrc

# V8 coverage output — the raw input to scripts/coverage-report.mjs, regenerated on every measured run
.coverage/
