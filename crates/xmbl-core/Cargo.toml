[package]
name = "xmbl-core"
description = "XMBL node runtime — orchestrates identity, networking, cubic ledger, state machine, consensus, and storage/compute."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true
readme = "README.md"
keywords = ["xmbl", "blockchain", "post-quantum"]
categories = ["cryptography::cryptocurrencies"]

[dependencies]
sha2.workspace = true
serde.workspace = true
serde_json.workspace = true
xmbl-identity = { version = "0.1.0", path = "../xmbl-identity" }
xmbl-networking = { version = "0.1.0", path = "../xmbl-networking" }
xmbl-cubic-ledger = { version = "0.1.0", path = "../xmbl-cubic-ledger" }
xmbl-state-machine = { version = "0.1.0", path = "../xmbl-state-machine" }
xmbl-consensus = { version = "0.1.0", path = "../xmbl-consensus" }
xmbl-storage-compute = { version = "0.1.0", path = "../xmbl-storage-compute" }
xmbl-zero-knowledge = { version = "0.1.0", path = "../xmbl-zero-knowledge" }
